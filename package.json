{
  "name": "sp-autoplan",
  "version": "1.5.1",
  "description": "AutoPlan plugin for Super Productivity",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "build:zip": "node build.js && cd plugin && zip -r ../sp-autoplan-plugin.zip .",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "keywords": [
    "super-productivity",
    "plugin",
    "task-scheduler"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "terser": "^5.46.0",
    "vitest": "^3.2.4"
  }
}
